# Generated with pipelinit 0.4.0
# https://pipelinit.com/
format:
  stage: sast-javascript
  image: returntocorp/semgrep-action@v1
  needs: ["format", "lint"]
  only:
    refs:
      - merge_requests
    changes:
      - "**/*.js"
      - "**/*.ts"
  script: semgrep-agent
  variables:
    SEMGREP_RULES: >-
      p/ci