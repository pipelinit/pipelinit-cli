# Generated with pipelinit 0.4.0
# https://pipelinit.com/
format:
  stage: format
  image: node:16
  needs: []
  script:
    - npx prettier --no-error-on-unmatched-pattern --check "**/*.yaml" "**/*.yml"
  only:
    refs:
      - merge_requests
    changes:
      - "**/*.yaml"
      - "**/*.yml"